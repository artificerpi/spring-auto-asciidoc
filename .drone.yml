name: default

kind: pipeline
type: docker

steps:
- name: gradle-builder
  image: gradle
  commands:
    - gradle -x functionTest clean build
    - gradle publishPlugins -Pgradle.publish.key=$gradlePublishKey -Pgradle.publish.secret=$gradlePublishSecret
